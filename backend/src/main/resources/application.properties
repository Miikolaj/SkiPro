# Spring Boot logging
# Set to DEBUG to see endpoint mapping and HTTP handling details
logging.level.org.springframework.web=DEBUG
logging.level.org.springframework.web.servlet.mvc.method.annotation.RequestMappingHandlerMapping=TRACE
logging.level.org.springframework.boot.web.embedded.tomcat=INFO

# Request/response logging (commons-request-logging-filter)
# Enables a filter that can log headers/params (without body)
spring.mvc.log-request-details=true

# --- Database (PostgreSQL) ---
spring.datasource.url=jdbc:postgresql://localhost:5432/skipro
spring.datasource.username=skipro
spring.datasource.password=skipro

# Hibernate / JPA
#spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false

# Uncomment during development:
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.format_sql=true
# logging.level.org.hibernate.SQL=DEBUG
# logging.level.org.hibernate.orm.jdbc.bind=TRACE

# JWT
# Base64-encoded HMAC key (at least 256 bits for HS256)
app.jwt.secret=vsYFF0uXzjUCyeF4p9VnhtcLgXNICYDmflDSNRZnoLf
